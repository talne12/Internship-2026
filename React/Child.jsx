import rract from "react";
export default function Child(Props) {
    return(
        <>
        <h1>This is child class of userState(parent )</h1>
        <h2>{Props.send}</h2>
        </>
    )
}